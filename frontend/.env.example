# Frontend (React) Configuration

# Base URL for the Flask API
REACT_APP_API_BASE=http://localhost:5000

# Firebase Web SDK config (populate from Firebase console)
REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_PROJECT_ID=
REACT_APP_FIREBASE_STORAGE_BUCKET=
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=
REACT_APP_FIREBASE_APP_ID=

# Optional: document that AI chat relies on the backend OpenAI key.
# Keep this empty on the client; the server reads its own OPENAI_API_KEY.
OPENAI_API_KEY=

# For production deployment, update the API base:
# REACT_APP_API_BASE=https://your-backend.example.com
