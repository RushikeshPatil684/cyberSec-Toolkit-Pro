# Flask / Backend Configuration
FLASK_ENV=production
FLASK_APP=app.py
SECRET_KEY=your_secret_key_here_replace_with_random_string

# Database (optional - for production with PostgreSQL)
# DATABASE_URL=postgres://username:password@host:port/dbname

# OpenAI API (optional - for AI assistant features)
OPENAI_API_KEY=

# Firebase Configuration
# Place your firebase-key.json file in the backend/ directory
# The app will automatically load it if present
